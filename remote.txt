git remote add origin <url>
원격 저장소를 추가한다
name은 보통 origin을 사용

git push <name> <branch> --> 밑에서는 git push origin master라고 씀
git push origin master 
내 저장소의 변경 내역(commit)을 원격에 푸시한다. origin에 master를 푸시한다.

git status
현재 변경 사항 내역 + staging area 표시